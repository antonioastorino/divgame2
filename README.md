# DivGame2 - a "canvasless" game

The game is displayed in an HTMLDivElement, a.k.a. `div`. Every element in the game is an HTML element. The logic is implemented in C, compiled to WebAssembly using `clang`.
